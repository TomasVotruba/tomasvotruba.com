<article class="blog-post">
    <header>
        <h2>
            <a href="{{ post.url }}">{{ post.title }}</a>
        </h2>

        <p>{{ post.meta.perex }}</p>

        {{ include('partials/postMetadataLine.twig') }}
    </header>
</article>
