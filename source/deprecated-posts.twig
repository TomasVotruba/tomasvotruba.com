---
layout: "_layouts/default.twig"
title: "Deprecated Posts"
id: blog
---

{% block content %}
    <div class="container-fluid" id="blog">
        <h1>Deprecated Posts</h1>

        {# get only deprecated posts #}
        {% set activePosts = [] %}
        {% for post in posts %}
            {% if post.deprecated is defined and post.lang is not defined %}
                {% set activePosts = activePosts|merge([post]) %}
            {% endif %}
        {% endfor %}

        {% include "_snippets/post/postList.twig" with { posts: activePosts } %}
    </div>
{% endblock %}
